DELETE t1 FROM contacts t1
INNER JOIN contacts t2 
WHERE 
    t1.id < t2.id AND 
    t1.email = t2.email;

SELECT name, region_id,  COUNT(*) c FROM kz_city GROUP BY name, region_id HAVING c > 1

DELETE t1 FROM kz_city t1 INNER JOIN kz_city t2 WHERE t1.region_id > t2.region_id AND t1.name = t2.name

// ok
DELETE n1 FROM kz_city n1, kz_city n2 WHERE n1.city_id < n2.city_id AND n1.region_id = n2.region_id AND n1.name = n2.name

// update region_id
UPDATE `kz_city` SET `region_id`=10 WHERE city_id > 416

// update coords
UPDATE `kz_city` SET coords='51.785262, 69.908818' WHERE region_id=1

// выбрать регионы и города по имени
SELECT kz_region.name, kz_city.name FROM `kz_city` inner join kz_region on kz_city.region_id= kz_region.region_id order by kz_region.name, kz_city.name
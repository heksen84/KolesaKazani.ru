1. При свободном месте сохранять вначале на диск, а потом в хранилище если место закончилось.
- Поместить case вместе с запросом в getImage()
- прописывать в images тип хранилища в котором сохранено изображение т.е. в storage_id
2. Вместо удаления записи в urls сделать пометку removed = 1, далее кроном удалить запись в sitemaps_xml. Нужно прописать sitemap_id, для привязки. В urls добавить sitemap_id.
3. В категории услуги удалить ценник
4. В adverts:deleteAll удалить все записи из sitemap.xml
5. Экспортнуть таблицу storages
6. По идее нужно перейти в другое хранилище изображений